# Запуск NDB Cluster в docker-compose
Сборка NDB Cluster с помощью docker-compose

Клонируем репозиторий в директориию.
```
cd docker_ndb_cluster/
docker-compose up -d
```

Конфиги находятся в папке mysql.conf
